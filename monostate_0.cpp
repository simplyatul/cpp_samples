/*
 * run: g++ monostate_0.cpp
 * monostate pattern imposes singularity thought behavior whereas
 * singleton pattern imposes singularity thought structure 
 *
 * In monostate pattern, all members (methods and variables) of a class
 * are declared as static. This creates a single state without having to
 * create an instance of a class. 
 */
